# odin-recipes
A simple 'recipe book' webpage built to showcase the following skills:
- basic HTML webpage structure
- basic CSS styling
- ordered & unordered lists
- a and img tags
- repo structure/git commits (probably could've done more of those, but I get so focused on just writing the code lol)